apiVersion: v1
kind: Service
metadata:
  name: ecommerce-service
  namespace: ecommerce-ns
spec:
  type: LoadBalancer
  selector:
    app: ecommerce
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
